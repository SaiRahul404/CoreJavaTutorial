package org.example;

public class TestStudent {

    String name;

    int rollNumber;

    TestStudent() {
        name = "Sample Name";
        rollNumber = 0;
    }

    TestStudent(String name, int rollNumber) {
       this.name = name;
       this.rollNumber = rollNumber;
    }
    // The this keyword is a reference to the current object — the object whose method or constructor is being
}
