package org.example;

public class InheritanceExample {
    // Inheritance is one of the four main pillars of Object-Oriented Programming (OOP), along with encapsulation, abstraction, and polymorphism

    //  Inheritance is the mechanism in Java by which one class (child or subclass) can inherit the properties (fields) and behaviors (methods) of another class (parent or superclass).

    // It allows code reusability.

    // class Animal { // parent class (Super class)
    //    void makeSound() {
    //        System.out.println("Animal makes a sound");
    //    }
    //}

    // // Subclass
    //class Dog extends Animal {

    //    void bark() {
    //        System.out.println("Dog barks");
    //    }
    //}

    // public static void main(String[] args) {
    //        Dog dog = new Dog();
    //        dog.makeSound();  // Inherited method
    //        dog.bark();       // Subclass-specific method
    //    }

    // Code Reusability – Write common code once in a superclass and reuse it in multiple subclasses.
}
